class UIPaths {
    // Auth
    login = '/login';
    logout = '/logout';
    register = '/register';

    // Insight
    insights = '/insights';
}

const uipaths = new UIPaths();
export default uipaths;
