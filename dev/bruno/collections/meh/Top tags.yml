info:
  name: Top tags
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8000/api/analytics/top-tags
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
