info:
  name: Partial Update
  type: http
  seq: 6

http:
  method: PATCH
  url: http://localhost:8000/api/v1/insights/11/
  body:
    type: json
    data: |-
      {
        "tags": []
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
