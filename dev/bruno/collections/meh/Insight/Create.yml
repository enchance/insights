info:
  name: Create
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8000/api/v1/insights/
  body:
    type: json
    data: |
      {
        "title": "eeeeeeeeeeeeee",
        "category": "alternatives",
        "body": "eeeeeeeeeeeeee",
        "tags": ["AAA", "BBB"]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
