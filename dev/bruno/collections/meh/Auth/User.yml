info:
  name: User
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:8000/auth/login/
  body:
    type: json
    data: |+
      {
        "username": "foo",
        "password": "pass123"
      }

  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
