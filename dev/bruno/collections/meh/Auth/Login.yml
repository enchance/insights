info:
  name: Login
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8000/auth/jwt/login/
  body:
    type: json
    data: |-
      {
        "username": "admin",
        "password": "pass123"
        
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
