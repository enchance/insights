info:
  name: Register
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8000/auth/users/
  body:
    type: json
    data: |-
      {
        "username": "bar",
        "email": "bar@email.com",
        "password": "pass123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
