info:
  name: Register
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8000/auth/users/
  body:
    type: json
    data: |
      {
        "username": "foo",
        "email": "foo@email.com",
        "password": "pass123",
        "re_password": "pass123"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
